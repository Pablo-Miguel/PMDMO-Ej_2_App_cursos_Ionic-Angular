import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PMDMO-Ej2_App_cursos_IONIC+Angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
